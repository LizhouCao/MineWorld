using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArkBuildingController : MonoBehaviour
{
    public ArkBuildingData buildingData {
        get { return m_buildingData; }
    }

    private ArkBuildingData m_buildingData = new ArkBuildingData();

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
